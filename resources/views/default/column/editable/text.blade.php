<a href="#"
    class="inline-editable"
    data-name="{{ $name }}"
    data-value="{{ $value }}"
    data-url="{{ $url }}"
    data-type="text"
    data-pk="{{ $id }}"
    style="text-align:right; color: green;"
>{{ $value }}</a>

{!! $append !!}
