<a href="#"
    class="inline-editable"
    data-name="{{ $name }}"
    data-value="{{ $value }}"
    data-url="{{ $url }}"
    data-type="textarea"
    data-pk="{{ $id }}"

    {{ $attributes }}

>{{ $value }}</a>

{!! $append !!}
