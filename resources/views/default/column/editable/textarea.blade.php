<a href="#"
    {!! $attributes !!}
    data-name="{{ $name }}"
    data-value="{{ $value }}"
    data-url="{{ $url }}"
    data-type="textarea"
    data-pk="{{ $id }}"
    data-mode="{{ $mode }}"
>{{ $value }}</a>

{!! $append !!}
