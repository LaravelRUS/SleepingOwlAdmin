# Tailwind view boundary

This directory is intentionally sparse. `sleeping_owl_tailwind` searches this
theme-owned path first and then the package `resources/views` root. Add a Blade
file here only when Tailwind requires structurally different markup;
presentation differences belong to `resources/css/themes/tailwind`.
