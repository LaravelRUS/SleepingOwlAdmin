# AdminLTE view boundary

This directory is intentionally sparse. `sleeping_owl_adminlte` searches this
theme-owned path first and then the package `resources/views` root. Add a Blade
file here only when AdminLTE requires structurally different markup; presentation
differences belong to `resources/css/themes/adminlte`.
