# Tabler view boundary

This directory is intentionally sparse. `sleeping_owl_tabler` searches this
theme-owned path first and then the package `resources/views` root. Add a Blade
file here only when Tabler requires structurally different markup; presentation
differences belong to `resources/css/themes/tabler`.
