# Theme stylesheet overrides

Application- or package-local theme corrections belong in a child directory
named after the canonical theme name. Override entries are loaded after the
selected theme and its feature adapters; archived or shared styles do not
belong here.
