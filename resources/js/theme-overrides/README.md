# Theme JavaScript overrides

Application- or package-local theme runtime corrections belong in a child
directory named after the canonical theme name. Override entries are loaded
after the selected theme and its feature adapters; shared drivers do not
belong here.
