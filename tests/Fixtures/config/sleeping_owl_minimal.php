<?php

declare(strict_types=1);

/*
 * A valid user config that relies on every package default except the title.
 * Future config keys must remain optional for this fixture.
 */
return [
    'title' => 'Minimal Admin',
];
